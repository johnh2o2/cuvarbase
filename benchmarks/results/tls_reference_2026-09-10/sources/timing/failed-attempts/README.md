These attempts are retained as failed timing preflights and supply no speed denominators.

- `unmapped_host_pid`: the earlier single-source attempt rejected an NVML host PID absent from the visible container PID list before any warmup. Ownership of that PID was not established.
- `unexpected_gpu_process`: the revised full campaign proved empty-device/worker-birth/call/clean-exit ownership for candidate1 and native1/2. During native4 preflight, another GPU PID appeared and remained after the four owned workers exited. All sixteen returned case outputs matched their frozen search fingerprints, but the exclusivity gate failed. The process identity was not established. No headline repetitions or component measurements ran.

The full campaign retry uses unchanged numerical sources and unchanged ownership, output and repetition gates. Exact execution-source and allocation receipts are preserved separately.
